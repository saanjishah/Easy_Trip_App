# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'finalProjectOutline' do
  # Comment the next line if you don't want to use dynamic frameworksuse_frameworks!

  # Pods for finalProjectOutline
pod 'Firebase/Firestore'
pod 'Firebase/Auth'
pod 'Firebase/Firestore'
  target 'finalProjectOutlineTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'finalProjectOutlineUITests' do
    # Pods for testing
  end

end
